<UserControl x:Class="BlocksMapViewerModule.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:model="clr-namespace:BlocksMapViewerModule"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <model:ContentViewModel x:Key="ContentViewModel" />
    </UserControl.Resources>
    <Canvas x:Name ="ContentGrid" 
          MouseLeftButtonDown="Window_OnMouseLeftButtonDown"
          MouseMove="Window_OnMouseMove"
          MouseUp="Window_OnMouseUp">
            <model:DrawingCanvas x:Name="DrawingCanvas" DataContext="{StaticResource ContentViewModel}" Offset="{Binding Offset}" 
                                 Canvas.Left="{Binding Offset.X}" Canvas.Top="{Binding Offset.Y}" />
    </Canvas>
</UserControl>
